name: Test

on:
  push:
    branches: [main, "001-*"]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --group dev

      - name: Run linter
        run: uv run ruff check .

      - name: Run formatter check
        run: uv run ruff format --check .

      - name: Run type checker
        run: uv run mypy .

      - name: Run unit tests
        run: uv run pytest tests/unit -v --tb=short

      - name: Run integration tests (mocked)
        run: uv run pytest tests/integration -v --tb=short -m "not requires_auth"

      - name: Run contract tests
        run: uv run pytest tests/contract -v --tb=short


  chatgpt-npm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: chatgpt/package-lock.json

      - name: Install chatgpt deps
        working-directory: chatgpt
        run: npm ci

      - name: Build
        working-directory: chatgpt
        run: npm run build

      - name: Test
        working-directory: chatgpt
        run: npm test
